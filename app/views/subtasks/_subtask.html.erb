<section id="subtasks">
	Subtask: 
		<%= subtask.id %>
		<%= subtask.title %>
		<%= link_to("Edit", [subtask.task, subtask], remote: true)%>
		<%= link_to("Delete", [subtask.task, subtask], method: :delete, remote: true) %>
		<br>
</section>
