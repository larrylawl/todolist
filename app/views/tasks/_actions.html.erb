<%= raw(task.tag_list.map{|t| link_to("#{t}", tag_path(t), remote: true)}.join(", ")) %>
<% if task.details != "" %>
	<%= link_to(image_tag("dropdown.png"), task_path(task), remote: true)%>
<% end %> 
<%= link_to(image_tag("edit.png"), edit_task_path(task), remote: true)%>
