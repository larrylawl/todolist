<%= link_to(image_tag("edit.png"), edit_task_path(task), remote: true, class: "button")%>
<% if task.details != "" %>
	<%= link_to(image_tag("show.png"), task_path(task), remote: true, class: "button")%>
<% end %> 
<%= raw(task.tag_list.map{|t| link_to("#{t}", tag_path(t), remote: true, class:"button")}.join(", ")) %>