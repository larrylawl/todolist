<%= task.id %>
<%= task.title %>
<%= raw(task.tag_list.map{|t| link_to("#{t}", tag_path(t), remote: true)}.join(", ")) %>
<% if task.details != "" %>
	<%= link_to("Show", task_path(task), remote: true)%>
<% end %> 
<%= link_to("Edit", edit_task_path(task), remote: true)%>
<%= link_to("Delete", task_path(task), remote: true, method: :delete)%>