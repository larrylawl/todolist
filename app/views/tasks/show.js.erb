const task_id = <%= @task.id %>;

$(`#${task_id} #subtasks`).fadeToggle();

$("#subtask_todo").focus();
$(`form[action*=${task_id}]`).keypress(function(event) {
		if(event.which == 13) {
			$(this).trigger('submit.rails');
		}
});
